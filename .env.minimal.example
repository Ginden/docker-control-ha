# .env.minimal.example
# A minimal example .env file with essential configuration.

# Hostname of your MQTT broker. This is usually required.
MQTT_HOST=your_mqtt_broker_ip_or_hostname

# Optional: MQTT username and password if your broker requires authentication.
# MQTT_USERNAME=your_mqtt_username
# MQTT_PASSWORD=your_mqtt_password

# Optional: Path to the Docker socket. Only needed if not using the default /var/run/docker.sock
# DOCKER_SOCKET_PATH=/var/run/docker.sock

# Enable control actions (start, stop, restart) from Home Assistant.
# Set to "true" to enable, "false" to disable. Default is "false".
ENABLE_CONTROL=true
